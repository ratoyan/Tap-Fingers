<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- Advertising ID — required so the Yandex ads SDK can read the Google
         Advertising ID on Android 13+. Without it Play replaces the ID with
         zeros, breaking ad serving/attribution. Keep the Play Console
         "advertising ID" declaration set to YES to match this. -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" />
    <!-- Avatar picking uses the Android system Photo Picker (react-native-image-picker
         launchImageLibrary), which needs no storage/media permission. Declaring
         READ_MEDIA_IMAGES / READ_EXTERNAL_STORAGE would trigger Google Play's
         Photo & Video Permissions policy declaration for no benefit, so they're
         intentionally omitted. -->
    <uses-permission android:name="android.permission.CAMERA" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">
      <!-- Yandex Mobile Ads needs no app-level ID in the manifest: the unit IDs
           in src/utils/ads.ts identify the app. Nothing to declare here. -->
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:theme="@style/SplashTheme"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>
    </application>
</manifest>
